<h2><%= user.first_name %></h2>
<p><%= user.address %></p>
<%# <%= link_to costume_path(costumes_id) %>
<% costumes = @costumes.where(user_id: user.id) %>
<% costumes.each do |costume| %>
<p><%= link_to costume.name, costume_path(costume), class:"text-decoration-none"  %></p>
<% end %>
